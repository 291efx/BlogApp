package com.cibertec.blogapp.util;

public class RutaConstantes {
    // Ruta donde se almacenarán los archivos (carpeta que creaste)
    public static final String UPLOAD_DIR = System.getProperty("user.dir") + "/uploads";
}